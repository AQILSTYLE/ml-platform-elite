apiVersion: v1
kind: Service
metadata:
  name: inference-service
spec:
  type: LoadBalancer
  selector:
    app: inference
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
